// components/index.js: barrel for the shared UI layer.
// Loads the design primitives (Btn, Icon, Crest, Avatar, Card, …) and the role-aware
// chrome (TopNav, RoleSwitcher) onto window so every screen can pull them in.
import './components.jsx';
import './chrome.jsx';
